{
  "name": "@navic/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@navic/shared-models": "*",
    "@navic/gnss-core": "*",
    "@navic/sensor-fusion": "*",
    "@navic/navigation-core": "*",
    "@navic/routing-core": "*",
    "@navic/map-core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
